---
header-includes:
        - \usepackage{tikz}
        - \usetikzlibrary{automata,positioning} 
output:
  md_document:
    variant: markdown_github
---

<!-- README.md is generated from README.Rmd. Please edit that file -->

[![Build Status](https://travis-ci.org/JohnNay/fsm.png?branch=master)](https://travis-ci.org/JohnNay/fsm)

```{r, echo = FALSE}
knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#>",
  fig.path = "README-"
)
```

This R package ...

If you use this package, cite: "Nay John (2015). eat. R package version 0.1. https://github.com/JohnNay/eat".

Install and load the latest release of the package from GitHub:
```{r, eval=FALSE, include=TRUE}
install.packages("devtools")
devtools::install_github("JohnNay/fsm")
library(fsm)
```

Please cite the package if you use it with the text generated by:
```{r, eval=TRUE, include=FALSE}
devtools::install_github("JohnNay/fsm")
library(fsm)
library(printr)
```
```{r, eval=TRUE, include=TRUE}
citation("fsm")
```

Create fake data:
```{r, eval=FALSE, include=TRUE}
cdata <- data.frame(period = rep(1:10, 1000),
                    outcome = rep(1:2, 5000),
                    my.decision1 = sample(1:0, 10000, TRUE),
                    other.decision1 = sample(1:0, 10000, TRUE))
```

Estimate model with that data:
```{r, eval=FALSE, include=TRUE}
result <- evolve_model(cdata)
```

Use the summary method on the output of the evolve_model() function:
```{r, eval=FALSE, include=TRUE}
summary(result)
```

Check out the documentation for the main function of the package to learn about the options:
```{r, eval=TRUE, include=TRUE}
?evolve_model
````

